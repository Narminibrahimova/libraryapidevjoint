spring:
  application:
    name: libraryapidevjoint

  datasource:
    url: jdbc:postgresql://localhost:5432/librarysystemdevj
    username: postgres
    password: 12345
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
